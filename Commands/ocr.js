const Discord = require('discord.js');

module.exports = {
    name: 'ocr',
    description: 'returns with the recognised text on the selected picture',
    /**
     *
     * @param {Discord.ContextMenuInteraction} interaction
     * @param {string} command
     * @param {Discord.Client} bot
     */
    async execute(interaction, command, bot) {

    }
}